@model System.Collections.Generic.IEnumerable<string>

<div class='post_data'>
    @if (Model != null)
    {
        <span class='categories'>
        @foreach (var tag in Model)
        {
            @Html.ActionLink(tag, "Tag", new { tag })
            @Html.Raw(" ")
        }
        </span>
    }
	@Html.Partial("_Author")
</div> <!--end post data-->